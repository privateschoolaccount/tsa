<header>
    <button type="button" id="Left">Left</button>
    <button type="button" id="Right">Right</button>
</header>
<main>
    <h1 id="recipeName">Recipe</h1>
    <p id="recipeContent">
        
    </p>
</main>
<footer>
    <button>Share</button>
    <a href="settings" class="button">Settings</a>
</footer> 
<script>
    let currentRecipe = 0;
    const recipes = JSON.parse(localStorage.getItem("recipeCards"));
    function changeRecipe(){
        
        document.getElementById("recipeContent").textContent = recipes[currentRecipe].text;
    }
    changeRecipe();
    const buttonLeft = document.getElementById("Left");
    const buttonRight = document.getElementById("Right");

    buttonLeft.addEventListener("click", function() {
        currentRecipe+=1;
        if(currentRecipe >= recipes.length-1){
            currentRecipe=0;
        }
    });
    
    buttonRight.addEventListener("click", function() {
        currentRecipe-=1;
        if(currentRecipe <= 0){
            currentRecipe=recipes.length-1;
        }
    });
</script>
<style>
    body{
        width:100vw;
        height:100vh;
        margin:0;
        display:grid;
        grid-template-rows:1fr 4fr 1fr;
        background-color:white;
    }
    header,footer{
        width:100%;
        height:100%;
        display:grid;
        grid-template-columns:1fr 1fr;
    }
    .button{
        display: inline-block;
        padding: 10px 20px;
        background-color: #007BFF;
        color: white;
        text-decoration: none; underline;
        border-radius: 5px;
        cursor: pointer;
        text-align: center;
        font-size: 10vw;
        border: 2px solid black;
        font-family: serif;
    }
    button{
        display: inline-block;
        padding: 10px 20px;
        background-color: #007BFF;
        color: white;
        text-decoration: none; underline
        border-radius: 5px;
        cursor: pointer;
        text-align: center;
        font-size: 10vw;
        border: 2px solid black;
        font-family: serif;
    }
    .button:hover{
        background-color: #0056b3;
        
    }
    button:hover{
        background-color: #0056b3;
    }
    h1{
        text-align: center;
    }
</style>
